function Sp = ftranspose(S,field)
% function Sp = ftranspose(S,field)
%
% A brilliant idea.  Transpose a structure array around a given field.  The entries in
% the field will become fieldnames in the new structure, and vice versa.  An accum
% is implicit.

